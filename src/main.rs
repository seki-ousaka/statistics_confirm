use statistics_confirm::mean::*;
use statistics_confirm::median::*;
use statistics_confirm::mode::*;

/// exec entry point
fn main() {
    // mean basic
    mean::mean();

    // median basic
    median::median();

    // mode basic
    mode::mode();
}
